# Base URL API backend. Kosongkan saat pengembangan agar memakai proxy Vite
# (lihat vite.config.ts) yang meneruskan /api ke http://localhost:8080.
VITE_API_BASE_URL=/api/v1

# Target proxy saat menjalankan `npm run dev`.
VITE_API_PROXY=http://localhost:8080
